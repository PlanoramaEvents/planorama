<section class="section">
  <div class="columns is-centered">
    <div class="column is-8-tablet is-6-desktop is-4-widescreen">
      <div class="box">
        <div class="card-content">
          <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <h2 class="title has-text-centered is-size-3">Please login</h2>
            <%= f.fields_for 'email_addresses_attributes[1]' do |e| %>
              <div class="field">
                <%= e.label :email , class: "label"%>
                <div class="control">
                  <%= e.email_field :email, autofocus: true, autocomplete: "email", class: 'input' %>
                </div>
              </div>
            <% end %>
            <div class="field">
              <%= f.label :password, class: "label" %>
              <div class="control">
                <%= f.password_field :password, autocomplete: "current-password", class: 'input' %>
              </div>
            </div>
            <%= f.submit "Log in", class: "button is-medium is-primary is-fullwidth" %>
          <% end %>
        </div>
        <div class="field has-text-centered">
          <a href="/users/password/new" class="text-muted">Forgot password?</a>
        </div>
      </div>
    </div>
  </div>
</section>
